{
  "name": "watch-catalog",
  "version": "1.0.0",
  "private": true,
  "description": "A static catalog website for a personal watch collection.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "process-photos": "node tools/process-photos/process.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "sharp": "^0.35.2"
  }
}
